package metadatamodel

// MetadataDefinitionPublication carries the workspace boundary required to
// atomically project Identity definitions into operational directories.
type MetadataDefinitionPublication struct {
	WorkspaceID string
}
